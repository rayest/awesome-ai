import type { CatalogProfile } from '@/lib/knowledge'

export function ComparisonDirectory({ title, eyebrow, description, profiles }: { title:string; eyebrow:string; description:string; profiles:CatalogProfile[] }) {
  return <main className="site-main page-main compare-page"><header className="page-intro"><span className="page-eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></header>{profiles.length ? <div className="compare-table"><div className="compare-head"><span>名称</span><span>价格</span><span>可用性</span><span>上下文 / 形态</span><span>适合场景</span></div>{profiles.map((item)=><article key={item.slug}><div><strong>{item.name}</strong><small>{item.provider}</small>{item.latestChange?<i>{item.latestChange}</i>:null}</div><span>{item.pricing}</span><span>{item.availability}</span><span>{item.contextWindow}</span><span>{item.bestFor||item.summary}</span><details><summary>查看完整档案</summary><dl><div><dt>核心能力</dt><dd>{item.capabilities||'等待编辑补充'}</dd></div><div><dt>限制</dt><dd>{item.limitations||'等待编辑补充'}</dd></div><div><dt>API</dt><dd>{item.apiAvailable?'支持':'未标注'}</dd></div><div><dt>开源</dt><dd>{item.openSource?'是':'否'}</dd></div><div><dt>最后核验</dt><dd>{item.verifiedAt||'等待复核'}</dd></div></dl></details></article>)}</div>:<section className="empty-state"><h2>结构化档案正在整理</h2><p>管理员发布模型或工具档案后，会在这里形成横向对比表。</p></section>}</main>
}

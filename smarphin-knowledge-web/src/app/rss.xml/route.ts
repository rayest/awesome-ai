import { listArticles } from '@/lib/knowledge'

export async function GET() { const base = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000'; const items = (await listArticles()).slice(0, 50).map((item) => `<item><title>${escapeXml(item.title)}</title><link>${base}/articles/${item.slug}</link><description>${escapeXml(item.summary)}</description></item>`).join(''); return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>知序 AI 知识社区</title><link>${base}</link><description>值得长期阅读的 AI 知识</description>${items}</channel></rss>`, { headers: { 'Content-Type': 'application/rss+xml; charset=utf-8' } }) }
function escapeXml(value: string) { return value.replace(/[<>&'"]/g, (char) => ({ '<': '&lt;', '>': '&gt;', '&': '&amp;', "'": '&apos;', '"': '&quot;' })[char] || char) }

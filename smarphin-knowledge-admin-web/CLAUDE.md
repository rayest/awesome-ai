# CLAUDE.md

AI 知识社区运营管理端。

- 技术栈：React 19、Vite、TypeScript、Ant Design 5、Pro Components、Zustand。
- 页面保持密集、可扫描、可批量操作，不使用营销 Hero。
- 表格、筛选、分页和表单优先使用 Ant Design/Pro Components。
- API 请求集中在 `src/api/`；跨页面会话状态放 store。
- 审核、发布、归档和删除必须确认并可审计。
- 使用陶土橙 `#b85c38` 作为有限的主操作色，危险操作保持语义红。

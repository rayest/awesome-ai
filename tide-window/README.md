# Tide Window

A minimal ocean image gallery built as a standalone Next.js frontend app.

Optional API keys:

```bash
NEXT_PUBLIC_PEXELS_API_KEY=...
NEXT_PUBLIC_UNSPLASH_ACCESS_KEY=...
```

Without API keys, the app uses a small fallback set of remote ocean images.

---
name: "text-repackager"
description: "二次修改用户粘贴的文本：拆解分析 + 重新组织语言系统与文章框架 + 联网获取最新信息 / 多元视角 + 优化类比比喻，确保准确性、精确性、可读性、系统性。Invoke when user pastes text and gives a command like 改写 / 重新组织 / 拆解 / 加点新东西 / 用更通俗的方式 / 二次创作."
---

# Text Repackager（文本二次修改与重组器）

## Purpose

把用户**粘贴 / 复制**过来的文本，按用户指定的命令**二次修改**：

1. **拆解分析**：识别原文结构、核心论点、薄弱环节
2. **重新组织语言系统**：换表达、换节奏、换视角
3. **重新组织文章框架**：重排段落、增删模块、调整逻辑链
4. **联网增强**（按需）：拉取最新数据、多元视角、行业动态
5. **类比优化**：用更贴切的生活化比喻替换生硬表达

**价值定位**：不是简单同义改写，而是**重构 + 升级**——产出物在准确性、精确性、可读性、系统性上**必须优于原文**。

## Trigger

用户**粘贴文本** + **给出修改命令**时立即触发。常见组合：

| 用户动作 | 触发命令示例 |
|----------|--------------|
| 粘贴一段论文 | "改写得通俗一点" / "加入更多案例" |
| 粘贴公众号文章 | "重新组织框架" / "换更正式的语气" |
| 粘贴技术博客 | "加点 2025 年的最新进展" / "补上对比维度" |
| 粘贴商业文案 | "换更精准的卖点表达" / "加入不同行业的应用案例" |
| 粘贴邮件草稿 | "改成更礼貌的版本" / "精简到 100 字" |

> 用户没指定具体命令时，**默认行为**：拆解 + 重组 + 联网补强 + 类比优化（四件套全开）。

## Workflow

### 1. 解析输入

从用户消息中提取：

| 要素 | 默认行为 | 可覆盖 |
|------|---------|--------|
| 原始文本 | 必填 | 无 |
| 修改命令 | 必填（如缺失，默认全开） | 用户指定 |
| 目标读者 | 与原文相同 | 用户指定（小白 / 专家 / 高管） |
| 目标长度 | 与原文相同 | 用户指定（"精简到 500 字"） |
| 输出语言 | 与原文相同 | 用户指定 |
| 联网开关 | 自动判断（涉及数据 / 趋势时开） | 用户强制 on/off |

### 2. 拆解分析

**对原文做 X 光扫描**（不展示给用户，仅作为内部加工依据）：

| 维度 | 提取内容 |
|------|----------|
| **结构** | 段落数 + 段落功能（引言 / 论点 / 案例 / 结论）+ 段落间逻辑 |
| **核心论点** | 3~5 个关键主张 |
| **薄弱环节** | 数据陈旧 / 论证不足 / 案例单薄 / 逻辑跳跃 / 类比生硬 |
| **可优化点** | 框架重排 / 模块增减 / 视角补全 / 比喻替换 |

### 3. 判断是否联网

**触发联网的条件**（满足任一即开）：

- 原文含具体数据 / 统计 / 时间点（需验证时效性）
- 原文涉及行业趋势 / 技术演进（需最新动态）
- 原文是单方观点（需补多元视角）
- 原文引用案例过旧（>2 年）
- 修改命令含"最新"、"当前"、"现在"、"补充"、"对比"

**联网工具**：用 `WebSearch` / `WebFetch` 拉 2~5 条权威源。

### 4. 二次修改原则

| 维度 | 原则 | 反例 |
|------|------|------|
| **准确性** | 数据 / 时间 / 引用必须核实 | 沿用原文错误数据 |
| **精确性** | 每个数字、术语、概念不模糊 | "很多人" / "效果不错" |
| **可读性** | 短句 + 节奏 + 重点突出 | 长段 + 学术腔 + 信息密度失控 |
| **系统性** | 框架完整 + 逻辑自洽 + 模块互不重叠 | 想到哪写到哪 + 同义反复 |

### 5. 语言重组策略

| 原文风格 | 重组方向 |
|----------|----------|
| 学术腔 | 拆长句 + 加口语化连接词 + 类比 |
| 公文腔 | 删套话 + 提炼动词 + 量化结果 |
| 技术 jargon 多 | 加白话翻译 + 必要时加括号注释 |
| 故事化但缺结构 | 抽时间线 + 加小标题 + 补数据 |
| 结构清晰但缺血肉 | 加案例 + 加数据 + 加对比 |

### 6. 框架重组策略

| 原文框架 | 重组方向 |
|----------|----------|
| 总分总（平淡） | 痛点钩子 → 颠覆认知 → 论证 → 升华 |
| 平铺直叙 | 倒叙 / 悬念 / 冲突 → 解决 |
| 单一视角 | 多视角并列 + 横向对比表 |
| 缺首尾 | 加开场钩子 + 收尾升华 |
| 案例散落 | 案例集中化 + 案例 → 洞察 → 应用 三段式 |

### 7. 类比优化

| 原文类比 | 优化方向 |
|----------|----------|
| 抽象术语堆砌 | 找一个生活场景（做饭 / 看病 / 装修 / 旅行） |
| 旧类比（>3 年） | 换新类比（外卖 / 短视频 / 直播） |
| 强行类比（不贴切） | 删除或换更贴的 |
| 单一类比贯穿 | 不同模块用不同类比，避免审美疲劳 |

### 8. 输出规范

**不写文件**（除非用户明确说"保存到文件"），**直接在对话中输出**。

**输出结构**：

```markdown
## 二次修改版本

[重组后的完整文本]

---

## 变更说明（可选，用户没问可省）

- **框架重排**：原 N 段 → 现 N 段，[核心调整]
- **语言系统**：[学术腔 → 口语化] / [公文化 → 故事化]
- **类比替换**：[X → Y]，原因：[更贴切 / 更新 / 更通俗]
- **联网补强**：[补了哪些数据 / 视角 / 案例]
- **删除冗余**：[删了哪些套话 / 同义反复]
- **新增模块**：[加了哪些段落 / 表格 / 列表]
```

**用户没要求时**：直接给二次修改版本，不写变更说明。

## Rules

- **绝不**大段照抄原文（最多保留 20% 关键句，其余必须重组）
- **绝不**沿用未经验证的数据 / 时间点（联网核实或标注"待核实"）
- **绝不**为改而改——如果原文已足够好，明确告诉用户"原文已优，建议保留"
- **绝不**丢失原文核心论点（重组不能改主旨）
- **绝不**添加原文没有的虚假案例（联网能补则补，补不上宁可不加）
- **绝不**省略联网环节的判断（涉及数据 / 趋势时必须决策 on/off）
- **绝不**改变原文的目标读者定位（除非用户明确说）
- **绝不**写"以上是我的改写"这类客套话
- 用户没说英文就**默认中文**
- 修改命令不明确时，**默认**全开（拆解 + 重组 + 联网 + 类比）

## Anti-patterns

- ❌ 不要做"同义替换"式改写（用近义词替换原词，没意义）
- ❌ 不要把原文段落顺序倒一下就交差（必须实质性重构）
- ❌ 不要忽略联网——涉及数据 / 趋势时联网是义务
- ❌ 不要使用过时类比（"就像当年的淘宝" → 2026 年已不贴切）
- ❌ 不要为了凑长度加废话（精简优于啰嗦）
- ❌ 不要把"重新组织"理解成"加几个小标题"
- ❌ 不要删除原文的数据 / 案例（要核实或替换，不能凭空消失）

## Quick Reference（速查）

| 输入类型 | 提取方式 | 备注 |
|---------|---------|------|
| 纯文本（粘贴） | 直接读取用户消息 | 必带修改命令 |
| 含 markdown | 解析结构后重组 | 保留或重排格式由用户决定 |
| 链接 / 图片 | 提示用户转文本 | 暂不支持直接处理 |
| 表格 | 保留表格但优化内容 | 必要时加列 / 改顺序 |

| 输出类型 | 字数参考 | 调整范围 |
|---------|---------|----------|
| 同等长度改写 | ±10% | 框架重组 + 语言优化 |
| 精简版 | 原文 30~60% | 砍冗余 + 保核心 |
| 扩展版 | 原文 150~200% | 加案例 + 加数据 + 加对比 |
| 通俗版 | 视原文而定 | 拆 jargon + 加类比 + 加场景 |

## 依赖

- `WebSearch` / `WebFetch`（按需启用）
- 无本地依赖

## 示例对话

**用户**：
```
[粘贴一段 2023 年的 AI Agent 综述文章]

帮我二次修改，融入 2025 年的最新进展。
```

**本 Skill 自动执行**：
1. 拆解：识别原文 5 段，结构为"概念 → 分类 → 应用 → 趋势"
2. 联网：搜 "AI Agent 2025 latest advances" + "Multi-Agent system 2025"
3. 重组：把 2023 年的趋势预测替换为 2025 年实际进展
4. 类比：把过时的类比（"像 PC 互联网"）换成更贴的（"像 App Store 生态"）
5. 输出：完整二次修改版本

**用户**：
```
[粘贴一段技术文档]

让小白也能看懂。
```

**本 Skill 自动执行**：
1. 拆解：识别 jargon 列表（"Transformer"、"注意力机制"、"上下文窗口"）
2. 不联网（技术概念无需实时数据）
3. 重组：每个 jargon 加白话翻译 + 生活类比
4. 类比：用"快递分拣"代替"注意力机制"等
5. 输出：小白友好版

**用户**：
```
[粘贴一段会议纪要]

精简到 500 字以内，保留核心决议和待办。
```

**本 Skill 自动执行**：
1. 拆解：识别会议时间 / 参会人 / 议题 / 决议 / 待办
2. 不联网
3. 重组：删寒暄 / 删讨论过程 / 留决议 + 待办
4. 输出：精简版（含 4 块：会议信息 / 核心决议 / 待办 / 截止日期）

**用户**：
```
[粘贴一段产品介绍]

改成更专业的 B2B 文案风格。
```

**本 Skill 自动执行**：
1. 拆解：识别卖点 / 客户痛点 / 差异化
2. 联网：搜竞品 + 行业最新术语
3. 重组：从故事化 → 数据化 + 客户证言 + ROI 测算
4. 输出：B2B 专业版

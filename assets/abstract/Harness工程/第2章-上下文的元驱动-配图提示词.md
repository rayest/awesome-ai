# 上下文的元驱动 · 配图提示词

> 用途：公众号 / 课程 / 视频封面 + 内文插图
> 图片总数：10 张
> 生成地址：`https://coresg-normal.trae.ai/api/ide/v1/text_to_image?prompt={URL编码}&image_size={尺寸}`
> 可用尺寸：`landscape_16_9`（封面/横幅）、`portrait_4_3`（章节插图）、`square_hd`（配图卡片）

---

## 图片 1：封面（米其林主厨比喻）

**尺寸**：landscape_16_9
**场景**：米其林主厨一天 100 桌客人的 5 大能力
**提示词**：

```
A cinematic wide-angle illustration of a Michelin chef in a busy kitchen serving 100 customers, surrounded by 5 distinct capability icons. Top-left "意图识别 Intent Recognition" (recognizing what guests want) with a magnifying glass on order slip. Top-right "计划模式 Plan Mode" (planning menu) with a recipe blueprint. Center-left "反思模式 Reflection" (revising dishes) with a pencil and feedback paper. Center-right "CodeAct 模式" (crafting custom tools) with a Swiss army knife. Bottom-center "人机协作 HITL" (human takeover) with a hand stop sign. Background: busy kitchen with 100 ticket orders floating. Color palette: warm kitchen orange (#ED8936) main, with 5 distinct accent colors for each capability (cyan, gold, red, purple, blue). Editorial cinematic style. 16:9 wide composition.
```

## 图片 2：意图识别医院分诊台

**尺寸**：landscape_16_9
**场景**：分诊护士根据症状分发科室
**提示词**：

```
A horizontal infographic illustration of a hospital triage desk. Center: a triage nurse with a clipboard, labeled "意图识别器 Intent Recognition". Left side: 3 incoming patients with symptoms - "胸口疼" (chest pain) → 分诊到 心内科 (cardiology), "孩子发烧" (child fever) → 分诊到 儿科 (pediatrics), "贵州茅台怎么样" (茅台 inquiry) → 分诊到 财经/礼品/收藏 (finance/gift/collection) with 3 dashed arrows showing ambiguity. Right side: 4 destination departments with signs. Bottom annotation: "分诊错了，后面再好的医生也治错病" (wrong triage = wrong treatment). Color palette: medical white and cyan (#4299E1) main, with red alert highlights (#E53E3E) for ambiguity cases. Flat design with hospital aesthetics. 16:9 composition.
```

## 图片 3：小模型 + 大模型双保险架构

**尺寸**：landscape_16_9
**场景**：两层意图识别漏斗
**提示词**：

```
A horizontal two-tier funnel architecture showing dual-model intent recognition. Top tier: "小模型 (Qwen2.5-1.5B)" labeled "第一道分诊" (first triage) with a small brain icon, fast and low-cost. Bottom tier: "大模型 (Qwen3-Max)" labeled "二级会诊" (second consultation) with a larger brain icon, handles edge cases. Connecting arrows show flow. Bottom decision diamond: "结果一致?" with two branches: "是" → 信任小模型 (trust small, save cost, green check), "否" → 触发兜底 (fallback trigger with 4 options: 礼貌拒绝 / 转人工 / 反问澄清 / 记录回流). Color palette: small model in cyan (#00D9FF), large model in gold (#D69E2E), decision flow in red (#E53E3E) and green (#48BB78). Modern editorial infographic style. 16:9 composition.
```

## 图片 4：计划模式 4 节点工作流

**尺寸**：landscape_16_9
**场景**：plan → execute → tool → should_continue 闭环
**提示词**：

```
A horizontal workflow diagram showing 4-node plan mode. Left to right: Node 1 "plan_node" (plan generation, brain icon) → Node 2 "execute_node" (LLM + tools, code brackets icon) → Node 3 "tool_node" (actual tool execution, gear icon) → Loop back to Node 2. Below Node 2: a decision diamond "should_continue" checking for "Final Answer" - if found → END (terminal icon), if tool_calls → back to tool_node, if neither → continue. Top annotation: "旅游攻略生成: 用户提问 → 计划模块 LLM → 大纲 → 执行模块 LLM + 工具 → 最终攻略". Color palette: cyan for plan (#00D9FF), gold for execute (#D69E2E), purple for tool (#7C3AED), red for end (#E53E3E). Technical workflow style. 16:9 composition.
```

## 图片 5：复杂计划模式 Pydantic 结构化

**尺寸**：portrait_4_3
**场景：竖向类图
**提示词**：

```
A vertical class diagram showing the Pydantic structured output for complex plan mode. Three classes stacked vertically: Top "Plan(BaseModel)" with field "steps: List[str]" (yellow). Middle "Response(BaseModel)" with field "response: str" (green). Bottom "Action(BaseModel)" with field "actions: Union[Response, Plan]" (orange). Arrows show inheritance and Union relationship. Side annotation: "绑定方式: llm.with_structured_output(Action) → LLM 只能输出合法 JSON". Bottom: a small flowchart showing dynamic plan shortening across rounds (Round 1: [A,B,C,D] → Round 2: [B,C,D] → Round 3: [C,D] → Round 4: [] → output). Color palette: class boxes in pastel colors with dark borders. UML-style class diagram. 4:3 portrait composition.
```

## 图片 6：反思模式双 LLM 架构

**尺寸**：landscape_16_9
**场景：生成器 + 反思器闭环
**提示词**：

```
A horizontal two-LLM reflection loop diagram. Top: "生成器 LLM" (generator) with brain icon, producing "初稿" (draft) flowing right. Right: "反思器 LLM" (reflector) with eye icon, producing "反馈" (feedback) flowing back to generator. Bottom: a feedback loop arrow showing iterative improvement. Right side: "4 维审查" (4-dimension audit) with 4 sub-boxes: 安全性 (security, red), 规范性 (compliance, gold), 效率性 (efficiency, cyan), 任务对齐 (task alignment, purple). Below the loop: "iterations ≤ 3 终止" (3 iterations max) with a counter icon. Example: 运维场景生成 docker run nginx command, generator → "docker run -d -p 8080:80 nginx" → reflector suggests "--name" → final improved command. Color palette: generator in cyan, reflector in gold, safety alerts in red. Editorial infographic style. 16:9 composition.
```

## 图片 7：CodeAct 三级沙箱防护

**尺寸**：square_hd
**场景：3 级防护盾牌
**提示词**：

```
A square infographic showing 3-tier sandbox protection. Three concentric shields, each labeled with risk level. Outermost "L3 容器化沙箱" (Container Sandbox) - "把刀锁进保险柜" (lock the knife in a safe) - shield with Docker icon (blue #2496ED), used in production. Middle "L2 受限解释器" (Restricted Interpreter) - "给刀装个保护套" (put a guard on the knife) - shield with code icon (gold #D69E2E), used in trusted internal tools. Innermost "L1 本地 exec" (Local exec) - "把刀放在桌上" (knife on the table) - shield with code icon (red #E53E3E), used for teaching/demo. Center: "用户代码" (user code) being passed through 3 layers. Bottom: "工业级系统必须采用 L2 或 L3" (Production must use L2 or L3). Color palette: gradient from red (inner, dangerous) to green (outer, safe). Security-themed editorial style. 1:1 square composition.
```

## 图片 8：HITL 两种触发形式

**尺寸**：landscape_16_9
**场景：自动驾驶 L2/L3 接管 + 强制 / 自主介入
**提示词**：

```
A horizontal comparison illustration showing two HITL triggering forms. Left "强制介入 (Mandatory)" - an ETC toll booth with mandatory checkpoint, a workflow stops at "大纲审核" (outline review) node marked "必停" (must stop). Right "自主介入 (Voluntary)" - a self-driving car dashboard showing "L2/L3 自动驾驶", with an LLM agent noticing missing info "想买几个?" calling an "ask_user" empty tool. Below: 4 elements of LangGraph interrupt: "interrupt() 喊暂停" / "memory 录像机" / "checkpointer 游戏存档" / "thread_id 哪一局游戏". Color palette: left in mandatory red (#E53E3E), right in voluntary gold (#D69E2E), background tech navy. Editorial flat design. 16:9 composition.
```

## 图片 9：五大模式叠加实例

**尺寸**：portrait_4_3
**场景：DeepResearch / Claude Code 公式分解
**提示词**：

```
A vertical Venn diagram showing 5 mode combinations. Top half "DeepResearch = Plan + Reflection + Search Tools" - showing 3 overlapping circles (plan mode gold, reflection mode cyan, search tools purple). Bottom half "Claude Code = CodeAct + HITL + Reflection" - showing 3 overlapping circles (CodeAct red, HITL orange, reflection cyan). Center: shared elements (LLM + State). Right side: stacking principle - "五种模式不是互斥的, 而是可以叠加" (5 modes are not mutually exclusive but stackable). Color palette: 5 distinct pastel colors for each mode with semi-transparent overlaps. Mathematical Venn diagram aesthetic. 4:3 portrait composition.
```

## 图片 10：决策树卡片（结尾图）

**尺寸**：portrait_4_3
**场景：Agent 卡点 → 推荐模式
**提示词**：

```
A vertical decision tree infographic card for selecting 5 Agent modes. Top question: "你的 Agent 卡在哪？" (Where does your Agent get stuck?). Five branches: "只是要听懂用户说什么" → "2.1 意图识别" (Intent Recognition). "任务可拆解成明确步骤" → "2.2 计划模式" (Plan Mode). "输出质量不稳定 / 容错率低" → "2.3 反思模式" (Reflection). "工具无法预定义 / 入参多变化" → "2.4 CodeAct 模式". "涉及高风险或信息缺失" → "2.5 Human-in-the-Loop". Bottom slogan: "Agent = 状态机 + LLM, 五大模式 = 状态机的不同边和节点组合" (Agent = State Machine + LLM, 5 modes = different edges and node combinations). Color palette: navy structure with color-coded branches. Editorial infographic style. 4:3 portrait composition.
```

---

## 配套图片清单（汇总表格）

| # | 主题 | 对应章节 | 尺寸 | 信息密度重点 |
|---|------|----------|------|--------------|
| 1 | 封面：米其林主厨 | 开场钩子 | landscape_16_9 | 5 大能力视觉化 |
| 2 | 医院分诊台 | 第一节 | landscape_16_9 | 意图识别类比 |
| 3 | 双模型兜底架构 | 第一节 | landscape_16_9 | 小 + 大模型协同 |
| 4 | 4 节点工作流 | 第二节 | landscape_16_9 | plan/execute/tool/should_continue |
| 5 | Pydantic 类图 | 第二节 | portrait_4_3 | Action / Plan / Response |
| 6 | 双 LLM 反思循环 | 第三节 | landscape_16_9 | 生成器 + 反思器闭环 |
| 7 | 三级沙箱防护 | 第四节 | square_hd | L1 / L2 / L3 强度递增 |
| 8 | HITL 两种触发 | 第五节 | landscape_16_9 | 强制 vs 自主介入 |
| 9 | 五大模式叠加 | 全章总结 | portrait_4_3 | DeepResearch / Claude Code 公式 |
| 10 | 决策树卡片 | 决策树段 | portrait_4_3 | 5 种卡点选型 |

---

## 使用建议

### 1. 生成顺序

- **首批优先**：图片 1（封面）+ 图片 4（4 节点工作流）+ 图片 10（决策树）→ 公众号首图三件套
- **正文穿插**：图片 2-3、5-9 按章节顺序使用
- **结尾复用**：图片 10 决策树可作为单独推送

### 2. 风格统一原则

- **主色调**：深蓝（#1A365D）+ 紫色（#7C3AED）+ 青色（#00D9FF）+ 金色（#D69E2E）
- **辅助色**：警示红（#E53E3E）、绿色成功（#48BB78）、橙色（#ED8936）
- **字体建议**：标题用思源黑体 / Inter，正文用思源宋体
- **图标风格**：保持线性 + 实心填充的混合，避免纯扁平

### 3. URL 编码提醒

生成图片时需对 prompt 进行 URL 编码：

```bash
python3 -c "import urllib.parse; print(urllib.parse.quote('你的 prompt'))"
```

完整调用模板：

```
https://coresg-normal.trae.ai/api/ide/v1/text_to_image?prompt={URL编码}&image_size=landscape_16_9
```

### 4. 公众号发布建议

- **封面图（图片 1）**：1200×675 像素，标题压在左下
- **正文图**：保持 16:9 比例，宽度撑满
- **决策树（图片 10）**：可单独做竖版海报
- **三级沙箱（图片 7）**：可作为信息安全相关的核心视觉
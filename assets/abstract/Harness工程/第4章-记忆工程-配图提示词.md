# 记忆工程 · 配图提示词

> 用途：公众号 / 课程 / 视频封面 + 内文插图
> 图片总数：10 张
> 生成地址：`https://coresg-normal.trae.ai/api/ide/v1/text_to_image?prompt={URL编码}&image_size={尺寸}`
> 可用尺寸：`landscape_16_9`（封面/横幅）、`portrait_4_3`（章节插图）、`square_hd`（配图卡片）

---

## 图片 1：封面（Agent 失忆瞬间）

**尺寸**：landscape_16_9
**场景**：用户与 AI 第二天重逢的尴尬瞬间
**提示词**：

```
A cinematic split-screen illustration showing an AI agent memory loss scenario. Left half: a user having a deep conversation with a glowing AI brain labeled "Day 1" surrounded by floating chat bubbles with detailed plans (travel, diet, work). Right half labeled "Day 2" shows the same user approaching a now-blank AI brain with question marks labeled "我不记得您说过的话" (I don't remember what you said). The AI brain has a cracked memory crystal in the center labeled "无状态 LLM" (stateless LLM). Color palette: warm orange (#F6AD55) on left for the past conversation, cold blue-gray (#718096) on right for the blank present. Editorial style with strong emotional contrast. 16:9 wide composition.
```

## 图片 2：人脑 vs AI Agent 记忆类比

**尺寸**：landscape_16_9
**场景**：人脑记忆层级 vs AI Agent 记忆层级对比
**提示词**：

```
A horizontal comparison illustration showing human memory (left) vs AI Agent memory (right) in three layers. Left "Human Brain": bottom layer "短期记忆" (Short-term) as a sticky note, middle layer "长期记忆" (Long-term) as a journal/diary, top layer "永久记忆" (Permanent) as a glowing brain section labeled "技能/本能" (skill/intuition). Right "AI Agent": bottom layer "上下文窗口" (Context Window) as a token counter, middle layer "外部存储" (External Storage) with vector database and graph database icons, top layer "模型微调" (Fine-tuning) as crossed out. Connecting arrows show "短期记忆" maps to "上下文窗口" and "长期记忆" maps to "外部存储". Color palette: warm beige (#F7FAF) for human side, cool tech blue (#2C5282) for AI side. Flat design with depth. 16:9 composition.
```

## 图片 3：滑动窗口 + 摘要策略

**尺寸**：portrait_4_3
**场景**：竖版时间线，展示滑动窗口压缩过程
**提示词**：

```
A vertical timeline infographic showing the sliding window + summarization strategy. From top to bottom: 1. "原始对话" (Raw Conversation) - 20 chat bubbles flowing down. 2. "滑动窗口" (Sliding Window) - a sliding frame that only keeps the latest 5-8 bubbles highlighted in cyan. 3. "早期对话 → 摘要" (Early Conversation → Summary) - older bubbles compressed into 3-4 key point cards labeled "关键事实1" "关键事实2" "关键事实3". 4. "合并上下文" (Merged Context) - the sliding window + summaries combined into a single chat panel. Side annotations show token count reduction. Color palette: dark navy background (#0A1929) with cyan highlights (#00D9FF) and gold summaries (#F6E05E). Technical dashboard style. 4:3 portrait composition.
```

## 图片 4：Mem0 4 层记忆架构

**尺寸**：landscape_16_9
**场景**：4 层堆叠的金字塔架构
**提示词**：

```
A pyramid-style architectural diagram showing Mem0's 4-layer memory architecture. From bottom to top: Layer 1 "对话层 Conversation" (transient, single request) with a chat bubble icon. Layer 2 "会话层 Session" (short-term, session level) with a clipboard icon. Layer 3 "用户层 User" (long-term, persistent) with a user profile icon. Layer 4 "组织层 Organization" (global, shared) with a building icon. Right side annotations show: "瞬时 → 短期 → 长期 → 全局" lifecycle progression. Left side shows analogies: "便签 → 日记 → 档案 → 图书馆" (sticky note → diary → file → library). Color palette: gradient from light cyan at bottom (#BEE3F8) to deep purple at top (#553C9A). Editorial flat design. 16:9 composition.
```

## 图片 5：Mem0 3 阶段生命周期

**尺寸**：landscape_16_9
**场景**：3 阶段流程图
**提示词**：

```
A horizontal three-stage process diagram showing Mem0's memory lifecycle. Stage 1 "语义提取" (Semantic Extraction) - a funnel filtering raw conversation into 7 fact categories labeled "个人偏好" "重要信息" "计划意图" "服务偏好" "健康信息" "专业信息" "杂项". Stage 2 "冲突仲裁" (Conflict Arbitration) - a state machine diagram with 4 operation nodes labeled "ADD" (green plus), "UPDATE" (yellow refresh), "DELETE" (red X), "NONE" (gray checkmark). Stage 3 "混合存储" (Hybrid Storage) - two parallel cylinders labeled "Qdrant 向量库" (vector database) and "Neo4j 图库" (graph database) with a connecting line labeled "双重检索" (dual retrieval). Color palette: cyan for Stage 1, gold for Stage 2, purple for Stage 3. Editorial infographic style. 16:9 composition.
```

## 图片 6：Mem0 完整工作流代码截图

**尺寸**：portrait_4_3
**场景**：竖版代码块展示
**提示词**：

```
A vertical code editor mockup showing the Mem0 Python code implementation. The code displays in dark theme with syntax highlighting: import statements at top, a config dict with 4 nested sections (llm, embedder, vector_store, graph_store), then `memory = Memory.from_config(config)`, followed by `memory.add(conversation, user_id="demo-user")` and `memory.search(query, user_id="demo-user", limit=3, rerank=True)`. Side annotations point to each section with labels "LLM 语义分析" "Embedder 向量化" "Qdrant 存储" "Neo4j 图谱". Color palette: dark gray background (#1A202C) with cyan code text (#00D9FF), green strings (#48BB78), yellow keys (#F6E05E), and orange annotations (#ED8936). IDE-style mockup. 4:3 portrait composition.
```

## 图片 7：3 节点 Qdrant 集群 + Redis 缓存架构

**尺寸**：landscape_16_9
**场景**：3 节点集群 + Redis 缓存层分布式架构
**提示词**：

```
A wide distributed system architecture diagram showing 3-node Qdrant cluster with Redis cache layer. Top center: "应用层" (Application Layer) with arrows pointing down. Middle layer: "Redis 缓存" (Redis Cache) as a single red cylinder labeled "写时失效 + 读时回填". Bottom layer: 3 interconnected Qdrant node cylinders labeled "Node 1" (主节点, with --uri flag) "Node 2" (从节点, with --bootstrap flag) "Node 3" (从节点, with --bootstrap flag). Connecting lines between nodes labeled "P2P 协议" "自动分片" "冗余存储". Right side: a failure scenario showing Node 2 grayed out with annotation "单节点失效 → 集群仍可服务". Color palette: dark background with red Redis, blue Qdrant nodes, gold connecting lines. Technical architectural style. 16:9 composition.
```

## 图片 8：缓存读写流程时序图

**尺寸**：landscape_16_9
**场景**：3 角色时序图（应用 / Redis / Qdrant）
**提示词**：

```
A horizontal sequence diagram showing the cache read/write flow. Three lifelines: "应用" (Application) on left in blue, "Redis 缓存" (Redis Cache) in middle in red, "Qdrant 集群" (Qdrant Cluster) on right in blue. Read flow (top arrow): App → Redis "GET cache_key" → Cache HIT "返回结果" → App. Read flow (miss, bottom): App → Redis "GET cache_key" → Cache MISS "查询 Qdrant" → Qdrant "返回结果" → App "SETEX cache_key TTL result" → Redis. Write flow (bottom): App "写新记忆" → Qdrant "存储成功" → App "DEL mem0:search:{user_id}:*" → Redis "缓存已清空". Color palette: blue for App/Qdrant (#3182CE), red for Redis (#E53E3E), with green HIT labels and yellow MISS labels. Technical sequence diagram style. 16:9 composition.
```

## 图片 9：Mem0 4 类事实分类示例

**尺寸**：square_hd
**场景**：分类卡片网格
**提示词**：

```
A square 7-card grid showing the 7 categories of facts Mem0 extracts. Each card has an icon and Chinese label. Card 1: coffee cup icon "个人偏好" (Personal Preferences) - example "喜欢黑咖啡". Card 2: ID card icon "重要信息" (Important Info) - example "姓名/关系/重要日期". Card 3: calendar icon "计划意图" (Plans & Intentions) - example "下个月去日本旅游". Card 4: restaurant icon "服务偏好" (Service Preferences) - example "不吃辣". Card 5: heart icon "健康信息" (Health Info) - example "低糖饮食". Card 6: briefcase icon "专业信息" (Professional Info) - example "10年产品经理". Card 7: book icon "杂项" (Miscellaneous) - example "最近在读《三体》". Color palette: pastel cards with vibrant icons, each category with its own accent color (orange, blue, green, red, pink, purple, teal). Modern flat design. 1:1 square composition.
```

## 图片 10：决策树卡片（结尾图）

**尺寸**：portrait_4_3
**场景**：记忆方案选型决策树
**提示词**：

```
A vertical decision tree infographic card for memory solution selection. Top question: "你的需求？" (Your requirement?). Five branches leading to solutions: "当前会话上下文" → "上下文注入" with chat bubble icon. "多轮对话" → "滑动窗口" with horizontal lines icon. "长对话 + Token 敏感" → "滑动窗口 + 摘要" with compress icon. "跨会话个性化" → "Mem0" with brain icon. "复杂关系推理" → "Mem0 + Neo4j" with graph nodes icon. "企业高可用" → "3 节点 Qdrant + Redis" with distributed nodes icon. Bottom slogan: "Agent 记忆 = 短期 + 长期" (Agent memory = short-term + long-term). Color palette: navy structure (#2C5282) with color-coded branches. Editorial infographic style. 4:3 portrait composition.
```

---

## 配套图片清单（汇总表格）

| # | 主题 | 对应章节 | 尺寸 | 信息密度重点 |
|---|------|----------|------|--------------|
| 1 | 封面：Agent 失忆瞬间 | 开场钩子 | landscape_16_9 | 痛点引入（Day 1 vs Day 2） |
| 2 | 人脑 vs AI 记忆类比 | 核心比喻 | landscape_16_9 | 3 层记忆对照 |
| 3 | 滑动窗口 + 摘要 | 第二节 | portrait_4_3 | 短记忆优化策略 |
| 4 | Mem0 4 层架构 | 第四节 | landscape_16_9 | 对话/会话/用户/组织 |
| 5 | Mem0 3 阶段生命周期 | 第四节 | landscape_16_9 | 提取/仲裁/存储 |
| 6 | Mem0 完整代码 | 第五节 | portrait_4_3 | 配置四件套 |
| 7 | 3 节点集群 + Redis | 第六节 | landscape_16_9 | 分布式部署架构 |
| 8 | 缓存读写时序图 | 第六节 | landscape_16_9 | 写时失效 + 读时回填 |
| 9 | 7 类事实分类 | 第四节 | square_hd | Mem0 提取策略 |
| 10 | 决策树卡片 | 决策树段 | portrait_4_3 | 5 种场景选型 |

---

## 使用建议

### 1. 生成顺序

- **首批优先**：图片 1（封面）+ 图片 4（4 层架构）+ 图片 10（决策树）→ 公众号首图三件套
- **正文穿插**：图片 2-3、5-9 按章节顺序使用
- **结尾复用**：图片 10 决策树可作为单独推送

### 2. 风格统一原则

- **主色调**：深蓝（#0A1929）+ 青色（#00D9FF）+ 金色（#F6E05E）+ 紫罗兰（#553C9A）
- **辅助色**：警示红（#E53E3E）、绿色成功（#48BB78）、橙色（#ED8936）
- **字体建议**：标题用思源黑体 / Inter，正文用思源宋体
- **图标风格**：保持线性 + 实心填充的混合，避免纯扁平

### 3. URL 编码提醒

生成图片时需对 prompt 进行 URL 编码：

```bash
python3 -c "import urllib.parse; print(urllib.parse.quote('你的 prompt'))"
```

完整调用模板：

```
https://coresg-normal.trae.ai/api/ide/v1/text_to_image?prompt={URL编码}&image_size=landscape_16_9
```

### 4. 公众号发布建议

- **封面图（图片 1）**：1200×675 像素，标题压在左下
- **正文图**：保持 16:9 比例，宽度撑满
- **决策树（图片 10）**：可单独做竖版海报
